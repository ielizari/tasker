import { dateToString } from '../../../lib/date.utils'
import { Schema } from '../../infrastructure/repositories/browser/browserdb'
export class dbToApiDto {
    mapWorklogToApiWorklog(){
        
    }
}